name: Go Test and Coverage

on:
  push:
    branches: ["master"]
    paths:
      - "**.go"
      - "ui/**"
      - "tests/**"
      - "Makefile"
      - ".github/workflows/test.yml"
  pull_request:
    branches: ["master"]
    paths:
      - "**.go"
      - "ui/**"
      - "tests/**"
      - "Makefile"
      - ".github/workflows/test.yml"
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ^1.21

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python test dependencies
        run: pip install pytest dnspython requests

      - name: Build frontend
        working-directory: ui
        run: |
          npm install
          npm run build

      - name: Copy frontend build output
        run: |
          rm -rf application/webserver/frontend/files/*
          mv ui/dist/* application/webserver/frontend/files/
          mv application/webserver/frontend/files/fs/* application/webserver/frontend/files/

      - name: Run integration tests (Go + Python)
        run: make test

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./coverage.txt
